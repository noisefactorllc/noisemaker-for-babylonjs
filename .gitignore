node_modules/
parity/.venv/
*.log
.DS_Store
dist/

# Vendored published engine — fetched from shaders.noisedeck.app (vendor/fetch.sh), NEVER committed
# (same posture as node_modules: commit the fetch script + loader, not the downloaded bytes).
vendor/noisemaker/

# generated bundles / demo artifacts
parity/harness/bundle.js
examples/bundle.js
examples/demo.fatgraph.json

# parity transient outputs — keep the reused reference goldens only
parity/out/*
!parity/out/*.golden.png

# transient parity manifests
parity/_*.manifest

# third-party NoiseBLASTER corpus — local-only test fixtures (commit the harness, not the art)
parity/corpus/raw/
parity/corpus/programs/
parity/programs/corpus_*.dsl
parity/out/corpus_*
examples/cubemap-bundle.js
examples/demo.cubemap.fatgraph.json
