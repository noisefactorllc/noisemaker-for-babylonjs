node_modules/
parity/.venv/
*.log
.DS_Store
dist/

# generated bundles / demo artifacts
parity/harness/bundle.js
examples/bundle.js
examples/demo.fatgraph.json

# parity transient outputs — keep the reused reference goldens only
parity/out/*
!parity/out/*.golden.png

# transient parity manifests
parity/_*.manifest

# third-party NoiseBLASTER corpus — local-only test fixtures (commit the harness, not the art)
parity/corpus/raw/
parity/corpus/programs/
parity/programs/corpus_*.dsl
parity/out/corpus_*
